const recovery_request = require('./recovery-request');
const recovery_verify = require('./recovery-verify');

module.exports = {
  recovery_request: recovery_request,
  recovery_verify: recovery_verify
};